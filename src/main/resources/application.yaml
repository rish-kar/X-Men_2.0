spring:
  application:
    name: X-Men
  main:
    headless: false
  web:
    resources:
      static-locations: classpath:/static/
      cache:
        period: 0

server:
  port: 8081

logging:
  level:
    web: DEBUG
    org.springframework.web: DEBUG
